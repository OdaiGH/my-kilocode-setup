# Test Philosophy

- Think in failure modes first — happy path last.
- Test observable behavior, not internal implementation details.
- Each test should have one reason to fail.
- A test that never fails is worse than no test — always verify the test can catch the bug it targets.
