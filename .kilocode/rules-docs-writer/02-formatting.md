# Formatting Rules

- All code samples must be in fenced code blocks with a language tag.
- Headings follow a strict hierarchy: H1 for title, H2 for sections, H3 for subsections. Never skip levels.
- File paths, commands, and identifiers must be wrapped in backticks.
- Never modify source code files — documentation only.
- Prefer relative links over absolute links for internal references.
